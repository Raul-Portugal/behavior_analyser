# Core scientific and computer vision libraries
numpy
opencv-python
scipy
matplotlib
seaborn

# GUI Framework
PyQt6

# Utility for progress bars
tqdm

# Note: The application also uses the 'tkinter' library for file dialogs.
# On most systems (Windows, macOS), this is included with Python.
# On some minimal Linux distributions, you may need to install it separately
# using your system's package manager (e.g., 'sudo apt-get install python3-tk').